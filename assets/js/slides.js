(() => { const slides=[...document.querySelectorAll('[data-slide]')]; if(!slides.length)return; let i=0; const c=document.querySelector('[data-counter]'); const show=n=>{i=(n+slides.length)%slides.length; slides.forEach((s,j)=>s.hidden=j!==i); c.textContent=`${i+1} / ${slides.length}`;}; document.querySelector('[data-prev]').addEventListener('click',()=>show(i-1)); document.querySelector('[data-next]').addEventListener('click',()=>show(i+1)); document.addEventListener('keydown',e=>{if(e.key==='ArrowLeft')show(i-1); if(e.key==='ArrowRight')show(i+1);}); show(0); })();
